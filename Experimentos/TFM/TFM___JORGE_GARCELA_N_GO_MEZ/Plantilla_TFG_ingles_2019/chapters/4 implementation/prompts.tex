%----------
%	SKILLS Y SALIDA VERIFICABLE
%----------

Toda la metodología del sistema vive en ficheros \texttt{SKILL.md}: un documento
por unidad de conocimiento, con un encabezado (nombre y descripción) y el cuerpo
con las instrucciones. La descripción es lo que el agente ve antes de decidir si
carga la skill, y por tanto lo que gobierna el enrutado. El cuerpo solo se lee
cuando hace falta.

La elección de Markdown no es casual. Es el formato que adopta la convención
\textit{Agent Skills}~\cite{anthropic2025skills} y encaja de forma natural con un
LLM por dos razones. Primero, es texto plano estructurado por
convenciones ligeras (encabezados, listas, énfasis) muy presentes en los datos de
preentrenamiento, de modo que el modelo interpreta esa estructura sin necesidad de
un esquema rígido. Segundo, sigue siendo legible y editable por una persona: una
experta en comunicación puede revisar o ajustar una skill sin tocar código, y el
fichero se versiona como cualquier otro documento de texto. Así, el mismo artefacto
sirve de fuente para el modelo y de material auditable para el equipo.

\subsection{Tipos de skill}

Hay tres tipos, con papeles distintos:

\begin{itemize}
    \item \textbf{Skills de variable} (cinco). Contienen la metodología paso a
    paso de cada variable. Se generan automáticamente desde
    \texttt{variables.json}, de modo que el codebook y las skills nunca se
    desincronizan. Cada agente carga la suya.
    \item \textbf{Skills auxiliares} (tres), que cualquier agente carga bajo
    demanda: \texttt{guia\_regla\_inversion},
    \texttt{guia\_lenguaje\_inclusivo} y \texttt{verificar\_evidencias}.
    \item \textbf{Resúmenes de guías}. Condensaciones de las guías reales de
    lenguaje no sexista (Sección~\ref{rag}), disponibles como orientación rápida.
\end{itemize}

Como se ha comentado anteriormente, que las skills de variable se generen desde \texttt{variables.json}
(Sección~\ref{sec:zeroshot_iris}) tiene una consecuencia importante para la validez del
experimento: el nivel B0 y el nivel B1 parten del mismo fichero fuente. El
\textit{baseline} recibe ese contenido inyectado en el \textit{prompt} y el agente
lo carga como skill. El contenido es byte a byte el mismo, así que la comparación
aísla el mecanismo de entrega y no un cambio en la metodología.

La Figura~\ref{fig:iris-skill} muestra la anatomía de una skill de variable con el
ejemplo de \texttt{masc\_generico}: el encabezado (nombre y descripción), que es lo
único que el agente ve en el catálogo, y el cuerpo con la metodología, que solo se
carga bajo demanda.

\begin{figure}[H]
\centering
\includegraphics[width=1.2\textwidth]{Plantilla_TFG_ingles_2019/imagenes/skill_anatomia_blanco.pdf}
\caption{Anatomía de una skill de variable (\texttt{masc\_generico}), generada
desde \texttt{variables.json}: encabezado visible en el catálogo y cuerpo con la
metodología, que se carga bajo demanda.}
\label{fig:iris-skill}
\end{figure}

\subsection{Salida estructurada y verificación de evidencias}

Cada agente cierra con un objeto JSON de tres campos (\texttt{codigo},
\texttt{explicacion} y \texttt{evidencias}), descrito en la
Sección~\ref{sec:zeroshot_iris}. Sobre las evidencias se aplica una comprobación
automática: solo se conservan los fragmentos que aparecen \emph{literalmente} en
el texto original. Con ello se refuerza la trazabilidad exigida por el marco
\textit{human-in-the-loop}, ya que cada evidencia queda anclada al texto de
partida. La comprobación se hace, además, contra el \emph{mismo} texto que recibió
el modelo, de modo que no se descarta por error una
evidencia que sí era literal.
