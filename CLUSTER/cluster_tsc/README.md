# cluster_tsc

This is the library for launching processes on the DTSC Cluster using the new SLURM resource manager and task descriptions defined in JSON format.

The objective is to maintain compatibility with the previous JSON specifications originally defined for MESOS.

IT IS NOT COMPATIBLE WITH LAUNCHING PROCESSES ON THE OLD MESOS-BASED CLUSTER.